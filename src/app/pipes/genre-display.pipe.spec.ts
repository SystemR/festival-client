import { GenreDisplayPipe } from './genre-display.pipe';

describe('GenreDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
